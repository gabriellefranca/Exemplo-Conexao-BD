<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gabi2.login">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.aluno.login.MainActivity" />
        <activity android:name="com.example.aluno.login.loading">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.aluno.login.UserProfile" />
        <activity
            android:name="com.example.aluno.login.Inicial"
            android:label="@string/title_activity_inicial"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.example.aluno.login.Produtos" />
        <activity android:name="com.example.aluno.login.menu" />
        <activity android:name="com.example.aluno.login.Users" />
        <activity android:name="com.example.aluno.login.Usuarios" />
        <activity android:name="com.example.aluno.login.Veiculos" />
        <activity android:name="com.example.aluno.login.Veiculoinfo" />
        <activity android:name="com.example.aluno.login.Servicos" />
        <activity android:name="com.example.aluno.login.Sad" />
        <activity android:name="com.example.aluno.login.Sobre" />
        <activity android:name="com.example.aluno.login.financi" />
        <activity android:name="com.example.aluno.login.CadastroProdutos" />
        <activity android:name="com.example.aluno.login.Editar_veiculo" />
        <activity android:name="com.example.aluno.login.Financiamento" />

        <uses-permission android:name="android.permission.VIBRATE" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name="com.example.aluno.login.AgendarTestDrive" />
        <activity android:name="com.example.aluno.login.AgendarManutencao" />
        <activity
            android:name="com.example.aluno.login.Main"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.example.aluno.login.localizacao"
            android:label="@string/title_activity_localizacao" />
        <activity android:name="com.example.aluno.login.Notificacao" />
        <activity android:name="com.example.aluno.login.Produtosinfo"></activity>
    </application>

</manifest>